import { Injectable } from '@angular/core';
import { Apollo } from 'apollo-angular';
import { observable,Subscription } from 'rxjs'
import gql from 'graphql-tag' 

@Injectable({
  providedIn: 'root'
})
export class PenyatuService {

  constructor() { }
}
